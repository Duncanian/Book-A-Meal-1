language: python
python:
  - "3.6.2"
  
# command to install dependencies
install:
  - "pip install -r requirements.txt"
  - pip install coveralls
script:
  - "python test_api.py.py"
after_success:
  - coveralls
notifications:
  email: false
